// For each is used to target each element of an array 
// It uses a call back funtion to create method .

const states = ["Bihar", "Rajasthan", "Up", 32, "asdkfh"];

states.forEach((s) => (console.log(s)));